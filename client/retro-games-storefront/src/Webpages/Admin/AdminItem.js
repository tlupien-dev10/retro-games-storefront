// import { useContext, useState, useEffect } from "react";
// import { useHistory } from "react-router-dom";

// import FormHelper from "../../Components/Forms/FormHelper";
// import AuthContext from "../../Components/AuthContext/AuthContext";

function AdminItem() {
return (
  <h1>Welcome to the Admin Inventory Page!</h1>
  // const [itemList, setItemList] = useState([]);

  // const adminView = useContext(AuthContext);
  // const history = useHistory();

  // useEffect(() => {
  //   if (adminView === null) {
  //     history.push ("/login");
  //   } else {
  //     const adminId = adminView.claims.jti;
  //     const jwt = adminView.jwt;

  //     fetch ("http://localhost:8080/api/admin/items")
  //   }
  // })
);
   }

export default AdminItem;
